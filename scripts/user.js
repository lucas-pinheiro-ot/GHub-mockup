
// Get login form input, verify with fictional account and register in local storage
function handleLogin() {
    var username = document.getElementById('username').value;
    var password = document.getElementById('password').value;
}

// Get register form input, register it in local storage then log user with it
function handleRegister() {
    return null;
}